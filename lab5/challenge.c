#include <stdio.h>

int main(){

    int i=0,n=0,j,k;


    printf("Enter number of elements: ");
    scanf("%d",&n);

    int num[i];

    for(i=0 ; i < n ; i++){


    printf("Enter %d integers: ", n);
    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    }/*
    for(i=0 ; i < n ; i++){
        printf("%d ",num[i]);
    }*/
    return 0;
}